# Monolog talks to Notion exclusively through its Rust backend, which reads the
# API key from the OS keychain — no environment variable is needed at runtime.
#
# This file is kept for local experimentation with the Notion API via curl etc.
# Copy to .env, never commit the real value.
NOTION_API_KEY=
